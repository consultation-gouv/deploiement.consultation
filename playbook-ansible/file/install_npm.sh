#!/bin/bash
sudo -u ogptoolbox bash -lc "curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -"
sudo -u ogptoolbox bash -lc "sudo apt install -y nodejs"
sudo -u ogptoolbox bash -lc "sudo apt install -y build-essential"
